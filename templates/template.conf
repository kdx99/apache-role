<VirtualHost *:80>
	ServerName {{ virtualhost }}
	ServerAdmin {{ admin_email }}

	Include /etc/apache2/conf-available/rewrite-http-to-https.conf

	CustomLog ${APACHE_LOG_DIR}/{{ virtualhost }}.log combined
	ErrorLog ${APACHE_LOG_DIR}/{{ virtualhost }}.log

	LogLevel {{ loglevel }}

</VirtualHost>
